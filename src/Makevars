PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"`
PKG_CXXFLAGS = -I. -DVERSION=\"1.7.6\"
PKG_CFLAGS = -I.

SOURCES = $(wildcard *.c */*.c */*/*.c)

OBJECTS = microsimulation.o fhcrc-example.o RngStream.o ssim.o $(SOURCES:.c=.o)
